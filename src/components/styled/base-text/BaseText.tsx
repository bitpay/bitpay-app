import styled from 'styled-components/native';
import {StyledComponentBase} from 'styled-components';

const BaseText: StyledComponentBase<any, any> = styled.Text`
  font-family: 'Heebo';
`;

export default BaseText;
